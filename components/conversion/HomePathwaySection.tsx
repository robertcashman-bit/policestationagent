/**
 * Pathways now live in HomeHeroCover so they appear on the first screen.
 * Kept as a named export for deep-link / import compatibility — renders nothing.
 */
export function HomePathwaySection() {
  return null;
}
