# Firm outreach emails — Police Station Agent

**Do not send firm outreach emails or any operator outreach digests from `policestationagent`.**

- Permanent kill-switch: `lib/firm-outreach/outreach-emails-disabled.ts` (`PSA_OUTREACH_EMAILS_DISABLED = true`)
- Env cannot re-enable (no `FIRM_OUTREACH_FORCE_SEND` escape hatch)
- Prospect/firm sends (cron, admin manual/bulk, Kent corrections, approval links) must stay blocked
- Operator digests blocked: Kent daily + **cross-workspace morning/evening** (`/api/cron/firm-outreach-cross-digest` removed from `vercel.json`; route is a no-op)
- Approval / confirmation / failure operator mail must stay blocked
- Admin firm-outreach UI/nav and send-approve pages are removed (404/410)
- Pipeline/enrich/discovery may still run for inventory only — never email
- Buffer and non-outreach crons stay on
- Do not flip the kill-switch to `false` unless Robert explicitly asks in writing to resume PSA outreach
